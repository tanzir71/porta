{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for Porta's main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
